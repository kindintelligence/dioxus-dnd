[package]
name = "dioxus-dnd"
version = "1.0.0"
edition = "2021"
rust-version = "1.85"
description = "Modular, accessible drag-and-drop for Dioxus: sortable lists, kanban boards, trees, grids, file drops, multi-select, touch support and more"
license = "MIT"
repository = "https://github.com/kindintelligence/dioxus-dnd"
readme = "README.md"
keywords = ["dioxus", "drag-and-drop", "dnd", "sortable", "ui"]
categories = ["gui", "wasm", "web-programming"]
exclude = [".github/"]

[package.metadata.docs.rs]
all-features = true

[dependencies]
dioxus = { version = "0.8.0-alpha.0", default-features = false, features = ["minimal"] }
serde = { version = "1", optional = true }
serde_json = { version = "1", optional = true }

[features]
default = []
## Typed DataTransfer payloads in `external::typed` (serde round-trip).
serde = ["dep:serde", "dep:serde_json"]

[dev-dependencies]
dioxus = { version = "0.8.0-alpha.0", default-features = false, features = ["minimal", "launch", "web"] }
dioxus-ssr = "0.8.0-alpha.0"
bytes = "1"
futures-util = { version = "0.3", default-features = false }
