{
  "scripts": {
    "pretest:web": "dx build --example regressions --platform web --features web,serde",
    "test:web": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
